[package]
name = "flora"
version = "0.1.0"
edition = "2024"

[dependencies]
spin = "0.12.2"
uefi = { version = "0.39.0", features = ["alloc", "global_allocator", "panic_handler"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
